# Prime Flight — task board (single source of truth)

Statuses: `todo` · `in-progress` · `review` · `blocked` · `done` · `dropped`. Tags: `trigger-change`, `decision-needed`, `external`.
ID: `PF-Qn-nn`. Task note — `tasks/notes/<ID>.md`. Updated by the owner or `pm-coordinator`.
State as of 2026-09-14 (updated 12:30): **the lead's draft** — to be aligned with Maksym Ch., Yurii, Aryan; after the review by Ihor/Serhii remove the draft mark.
The "GM/Tracker/modules analysis" phase started on 14.09: tasks PF-Q1-12…17, reports in `docs/analysis/`. Code is assembled in the `pf/` package of this repo (GitHub — PF-X-04).

---

## Q1 — Continuous Upload + Faster Post-Processing (months 1–3)

| ID | Task | Owner | Agent | Status | Deps | Acceptance |
|---|---|---|---|---|---|---|
| PF-Q1-01 | Wi-Fi/SIM upload of chunks from the CameraBox **during recording** (camera_software: chunks_handling, storage_handling); the rest of the pipeline as is | Maksym Ch. | pipeline-architect | todo | PF-Q1-08, external: channel/VPN | a chunk is available in the bucket ≤ 30 s after the file is closed; merge and everything downstream work unchanged; Time-to-first-chunk measured on 1 gate |
| PF-Q1-02 | Chunk receiver + session registry: ordering, gap-fill with placeholder frames, `frame_id`, `schema_version` (reference `gat-streaming/streaming/session.py`, `contract.py`) | Maksym Ch. | pipeline-architect | todo | — | parity tests: chunked feed = end-to-end bit-for-bit; a lost chunk does not shift the numbering (`drop-mode fill`) |
| PF-Q1-03 | Stage detector v0: T_arr, T_dep, BL@door, BL leave, `pushback_attached` (stationary bounding box for N s) as the sole owner; `stage/events/anchors` in the contract | Maksym Ch. | pipeline-architect | todo | PF-Q1-06 | events on every frame; latency 4 s/10 s; on the 8 stand videos the anchors match the batch ones ±1 frame (except the documented Nose wheel shift) |
| PF-Q1-04 | Remove the merge from the early stage: GM v2 + Tracker v2 run over chunks as they arrive via `pf.receiver.Session` (one tracker per event), modules receive frames "on the fly", merge only for the archive/post modules | Maksym Ch. (+Valentyn) | pipeline-architect | in-progress (foundation: `scripts/stream_pipeline.py` runs GM v2 + Tracker v2 on 60-frame chunks with causal second-run rows, tracker events and PUSHBACK_ATTACHED, and writes v2 bus frames with anchors — smoke on 4 200 frames at 1.9× real time under load, T_ARR at the production frame; full-video run and L2 on the streaming outputs queued; `docs/analysis/streaming_v0.md`) | PF-Q1-02, PF-Q1-16, PF-Q1-17 | verdicts of the E0 modules identical to the batch ones on a balanced sample; Time to Result measured before/after |
| PF-Q1-05 | GM: hygiene for chunk-by-chunk processing (no dependency on the full video: camera/aircraft-type/entity classification from the first N frames), model registry, dead models (wing-walkers wand), a foreign `model_name` in `__main__` | Yurii + Valentyn | gm-tracker-engineer | todo | — | GM works on a stream of chunks without merge; ndjson output bit-for-bit = batch |
| PF-Q1-06 | Tracker: `schema_version` in `state_dict`, field-leakage test (`VEHICLE_ONLY_KEYS`), one tracker per event without reset, events for the stage detector (BL@door, BL leave), `pushback_attached` causally from stationarity | Yurii + Valentyn | gm-tracker-engineer | in-progress (events v0: `pf/tracker/events.py` derives T_ARR, T_DEP, BL_AT_DOOR, BL_LEAVE causally from the published tracker records with door occupancy and re-identification — on the production file of DjwtQRdZyt0sSk T_ARR 3669 and the front door 9888–22776, exactly beltloader-chocks' timeline; one tracker per event and causal updates: `pf/tracker/stream.py`; record contract checks: `pf/tracker/contract.py`; `pushback_attached` ported causally in `pf/stage/pushback.py` — the same frame as production on the 5 of 7 ATL-C5 videos where production uses its causal nose rule, −183 / −18 frames on the 2 where it uses the look-ahead median rule; open: `schema_version`) | — | `check_class_leakage` green; 5 "aircraft" on the reference video; Nose wheel anchor shift ≤ 24 s or documented (D5) |
| PF-Q1-07 | Parity CI: level 1 gates in the repo pipeline + nightly on a GPU runner (`compare_runs.py`, exit 1 on a discrepancy) | Denys (+ a request to Ihor for a runner) | qa-parity | blocked `external` | runner | nightly goes red on a batch↔stream discrepancy for at least the 4 first-queue checks |
| PF-Q1-08 | Baseline Time to Result: measure the current ≈24 h by stages (upload, merge, GM, tracker, modules, delivery) on 10 events | Valentyn | pm-coordinator | todo | — | a table of stages with median/p90; agreed with Ihor as the baseline KPI |
| PF-Q1-09 | Fail labels for balanced samples: Main gear on all 30 fails; Nose wheel; BL forward chock; pushback-pathway (45 s window, transport classes) | Denys / Oksana | qa-parity | todo | — | `gt_by_video.json` covers all fails of the monthly reports for the 4 first-queue checks |
| PF-Q1-10 | Hierarchy GM → group → module and the shared components E01–E33 as a refactoring plan (what we move out of the modules into shared) | Vladyslav + Yurii | module-porter | todo | — | table "component → consumer modules → where the code lives now → where it goes"; review by Ihor |
| PF-Q1-11 | Delivery Q1: demo "video arrives while the CameraBox is recording" + Time to Result report before/after | Valentyn | pm-coordinator | todo | Q1-01…06 | demo to the client; report in `tasks/notes/PF-Q1-11.md` |
| PF-Q1-12 | **GM as-is analysis**: responsibilities, models, per-frame schema, per-video decisions (camera/type/entity), the "full video" assumption, performance profile, recommendations for v2 → `docs/analysis/gm_current.md` | Valentyn (+Yurii review) | gm-tracker-engineer | review | clone `external/general_model` @13a4ddc | report with file:line; the list of "what must stay bit-for-bit" for the modules |
| PF-Q1-13 | **Tracker + cv_common as-is analysis**: state_dict per class, arrival/departure events, BL semantics, reset, optical flow `_p0/_st` → `docs/analysis/tracker_current.md` | Yurii + Valentyn | gm-tracker-engineer | review (report `docs/analysis/tracker_current.md`, 389 lines; note `tasks/notes/PF-Q1-13.md`; production bd43c3c uses YOLO-seg masks, cv_common commit of the prod image unknown) | access to `cv_trackers`/`cv_common` (PF-X-05) | report + minimal `state_dict` contract |
| PF-Q1-14 | **Consumption matrix**: what each of the 27 modules reads from GM (classes/fields) and the tracker (`state_dict`, private fields), pixels, passes, pins → `docs/analysis/module_consumption.{md,json}` | Valentyn (+Vladyslav) | module-porter | review | clones of the 27 modules in `external/` | the union of GM classes and tracker fields = the v2 compatibility contract |
| PF-Q1-15 | **Baseline measurement and harness**: (a) speed ms/frame per component on a full turnaround; (b) accuracy: per-frame ndjson parity v1↔v2 (`pf.eval.compare_gm_ndjson`) + parity of module verdicts on the same videos + fail samples; data: ATL-C5 locally (7 videos) + buckets after `gcloud auth login` → `docs/analysis/measurement_plan.md`, `contract_observed.md` | Denys / Oksana / Valentyn | qa-parity | in-progress (contract_observed + inspect_ndjson.py ready; L1 GM measured on 1 full video; L2 on all 12 pixel-free modules (`docs/analysis/l2_gate.md`): 11 of 11 comparable modules give the same verdict on GM v2 inputs; speed harness `scripts/gm_bench.py` + `gm_ep_probe.py`; tracker profiling running) | PF-Q1-09 | methodology agreed with Ihor; the first baseline run on the 7 ATL-C5 videos |
| PF-Q1-16 | **GM v2** in `pf/gm` (per ADR-001: `gm_core` + `gm_context` + v1-compat/v2 sinks): a clean frame→detections core (`FrameDetector`), incremental `VideoContext` (camera/type/entity with a "decided at frame X" event), I/O adapters separate; bit-for-bit compatibility of the `[x1,y1,x2,y2,conf,class_id]` format and the class-id map | Yurii + Valentyn | gm-tracker-engineer | in-progress (GM v2 complete for the production variant; full-video L1 parity vs production 99.90 % paired rows incl. main-aircraft/obstacle rows, full turnaround with the fixed options and parallel heads 28.3 ms/frame including decode = 4.42× RT (pair recall vs production 99.90 %); speed for the three heads on the 5070 Ti 22.4 ms/frame with the v1 options, 16.8 ms with shared preprocessing + parallel heads (rows byte-identical), 8.0 ms with TensorRT (tolerant parity, L2 gate pending) — `docs/analysis/gm_speed.md`; L2 gate beltloader-chocks Pass/Pass; second pass ported (`camera_type`, `frame_stopped`; bit-identical to the inline v1 reproduction; validation against production GM reports pending — MongoDB; RT per ADR-002); next: parallel heads default in GmStream, TensorRT L2 gate, the other 6 ATL-C5 videos, chunk-wise run through Session with real chunks) | PF-Q1-12, PF-Q1-14, PF-Q1-15 | ndjson parity v1↔v2 on 7 videos (frame_parity ≥ 0.99 at ndigits=1 or every discrepancy explained); ≤ 28 ms/frame; module verdicts unchanged |
| PF-Q1-17 | **Tracker v2** in `pf/tracker` (per ADR-001: `state_dict` frozen, new things — in `data{}`/`anchors`): causal, one per event, `schema_version` in `state_dict`, events for the stage detector (BL@door/BL leave/pushback stationarity), no private fields in the contract | Yurii + Valentyn | gm-tracker-engineer | in-progress (**port proven**: `pf/tracker/stream.py` on the vendored production classes is byte-identical to the production pin under the same seed (1 200/1 200 lines), and so are the exact fast paths including grouped optical flow (−23 % under contention on both slices, idle re-run pending); the v2 bus without `_p0`/`_st` is 10.8× lighter; full video GM v2 → Tracker v2: stage events identical to production (T_ARR, BL at door / leave); v1 tracker cost baseline measured on two 1 200-frame slices — master pin 137–149 ms/frame (below real time; full-frame `estimate_sigma` = 100 ms of it), production pin 36–49 ms/frame (state machine: optical flow 7–11, DeepSORT 6–10, `estimate_sigma` 7, YOLO-seg 2–4); master vs production statuses differ on 2.8 % of object-frames; v2 design + target ≤ 15 ms/frame in `tasks/notes/PF-Q1-17.md`; `scripts/tracker_v1_profile.py`, `scripts/win_shims`; blockers: exact cv_common commit of the prod image (bd43c3c runs with `tracker_optimization` @2759daf — `Vehicle.update_params` matches; whether the image carried that commit or an earlier one of the line is open), unseeded RNG → determinism policy) | PF-Q1-13, PF-Q1-14 | `state_dict` parity on the consumed fields on 7 videos; 5 "aircraft" on the reference video; module verdicts unchanged |
| PF-Q1-18 | **Monthly test set on GM v2 + Tracker v2**: all modules on the 69 events / 90 videos of the monthly CI report (job 16263924101), twice — on the production inferences (control: GM 8576299 + tracker bd43c3c) and on the v2 inferences; task verdicts paired v2 vs control and vs the report's New output / validated truth; GM + Tracker cost v1 vs v2 | Valentyn (+Yurii) | qa-parity + gm-tracker-engineer | in-progress (orchestrator `scripts/testset/orchestrate.py` running since 14.09 18:51; event 1: control = New output on 11/12 pixel-free tasks, GM v2 pair recall 99.93 %, Tracker v2 objects 100 % matched; environments for the mmpose modules, hair-policy and the walk-arounds in progress — `tasks/notes/PF-Q1-18.md`) | PF-Q1-16, PF-Q1-17 | paired v2 vs control: every verdict change explained; accuracy vs truth not lower than the control; GM + Tracker cost per video lower than v1 on the same machine (clean run) |

## Q2 — Real-Time Architecture Foundation (months 4–6)

| ID | Task | Owner | Agent | Status | Deps | Acceptance |
|---|---|---|---|---|---|---|
| PF-Q2-01 | RT branch: frame-by-frame (not chunked) ingestion onto the GPU host, decoding on the spot, contract bus, module gating by the stage detector | Maksym Ch. | pipeline-architect | todo | PF-Q1-02, PF-Q1-03 | end-to-end latency frame → contract ≤ 125 ms (p95) on 1 camera; 3.95× RT on a full turnaround does not degrade |
| PF-Q2-02 | GM/Tracker for RT: RT-GM (≈6 classes), batching/ONNX or TensorRT, the 1080p vs 720p decision (recall −≈3 %), memory profile for N cameras | Yurii + Valentyn | gm-tracker-engineer | todo | PF-Q1-05, PF-Q1-06 | GM ≤ 28 ms/frame kept or improved; the number of cameras per 1 GPU documented |
| PF-Q2-03 | **First RT module**: `beltloader-chocks` (E0·I1, NOW, recall 83 %/precision 91 %) via the gate BL@door…BL leave | Valentyn / Yurii | module-porter | todo | PF-Q2-01 | batch↔stream parity 0 discrepancies on a balanced sample; the verdict at the moment of BL leave, before the merge |
| PF-Q2-04 | Hedge candidates without dependency on GM/Tracker changes: `pushback-does-not-start-until-wing-walkers…` (E0·I1), `bl_rear_cone`, `3-stop-brake-check` | Valentyn / Yurii | module-porter | todo | PF-Q2-01 | at least one additional module in RT with parity |
| PF-Q2-05 | Module gating in the RT branch (open/close by events, 4–9 active per stage instead of 27) | Maksym Ch. | pipeline-architect | todo | PF-Q1-03 | resource per event ↓ ≥ 2× vs always-on; lookback modules without a replay buffer |
| PF-Q2-06 | Delivery Q2: demo "a module delivers a result before the merge completes" | Valentyn | pm-coordinator | todo | PF-Q2-03 | demo to the client + a Time to Result measurement for the RT check |

## Q3 — Real-Time Expansion + Alerting (months 7–9)

| ID | Task | Owner | Agent | Status | Deps | Acceptance |
|---|---|---|---|---|---|---|
| PF-Q3-01 | Alert bus: deduplication, quiet windows, alert lifecycle, MongoDB schema, endpoint, rendering on RampVision (case: 357 alerts on a single video without deduplication) | Aryan | alerting-engineer | todo | PF-Q2-03 | ≤ 1 alert per violation event; latency verdict → alert ≤ 5 s; the list of open questions to Ihor closed |
| PF-Q3-02 | Wave E0 (12 modules "as is"): by weight I1 → I2 → I3 | Valentyn / Yurii / Vladyslav | module-porter | todo | PF-Q2-05 | each — parity + a measurement on fail videos; done status only after qa-parity |
| PF-Q3-03 | Wave E1 (frames without own models, 4 modules) | module-porter (assignment at the start of Q3) | module-porter | todo | PF-Q3-02 | the same + the per-frame cost measured |
| PF-Q3-04 | PATCH modules via a detector event: `aircraft-chocks` (main_stream, 0 discrepancies Main gear), `pin-verification` | Yurii | module-porter | todo | PF-Q1-03 | both single-pass; `pushback_attached` only from the detector |
| PF-Q3-05 | Architecture tech debt: unification of the `cv_common` pins in modules, gzip ndjson, registry (crew-present full_name, aircraft/jet), removal of the arrival-stage duplicates from 9 modules | Maksym Ch. | pipeline-architect | todo | PF-Q1-10 | a single cv_common pin for the E0/E1 modules; no local arrival stage computation whatsoever |
| PF-Q3-06 | `trigger-change` track for S1: safety zone (causal zone along the entry trajectory), pushback pathway (continuously during motion), walkers (alert on pushback_attached ∧ walkers absent), hand signals (two-pass → event) | Valentyn + Yurii | module-porter | todo `decision-needed` | Ihor/Oksana | an ADR per trigger change; the client logic in `05_module_logic.md` updated after agreement |
| PF-Q3-07 | Delivery Q3: more RT modules + alerts to station managers | Valentyn | pm-coordinator | todo | Q3-01…04 | demo + report |

## Q4 — Maximum Real-Time Coverage + Optimization (months 10–12)

| ID | Task | Owner | Agent | Status | Deps | Acceptance |
|---|---|---|---|---|---|---|
| PF-Q4-01 | Wave E2 (frames + models, 5; vests last) and RETHINK/E4 (hand-signals, safety-zone geometry from a buffer); POST modules deliberately stay in the post branch | module-porter | module-porter | todo | PF-Q3-03 | the 125 ms budget is met with all RT modules on 1 camera; the "deliberately POST" list approved |
| PF-Q4-02 | GM/Tracker optimization: TensorRT/fp16, resolution, a shared detector for the two branches without repeated inference | Yurii + Valentyn | gm-tracker-engineer | todo | PF-Q2-02 | cameras per GPU ↑ ≥ 1.5×; recall no worse than −1 % vs baseline |
| PF-Q4-03 | Stage detector v1 + architectural optimization (receiver, resources, stability, GKE autoscaling) | Maksym Ch. | pipeline-architect | todo | PF-Q3-05 | compute/event ↓, stability SLO documented |
| PF-Q4-04 | Gate streaming to RampVision (live gate view with stage/events/verdicts) | Aryan + Maksym Ch. | alerting-engineer | todo | PF-Q3-01 | a live gate page on the dev portal; delay ≤ 10 s |
| PF-Q4-05 | Post branch only for the inherently-POST (walk-arounds, conditioned air lookback) on the optimized pipeline | Maksym Ch. | pipeline-architect | todo | PF-Q1-04 | Time to Result of the post part ≤ 5–6 h (measured on 10 events) |
| PF-Q4-06 | Final KPI measurement vs baseline + the year report | Valentyn | pm-coordinator | todo | everything | report using the PF-Q1-08 methodology |

---

## Cross-cutting / support

| ID | Task | Owner | Agent | Status |
|---|---|---|---|---|
| PF-X-01 | Keep `docs/` up to date: after every ADR or xlsx change → `python scripts/build_docs.py`; new documents via `docs/inbox/` | Valentyn | pm-coordinator | ongoing |
| PF-X-02 | The list of open questions to Ihor in the channel (transport, VPN, GPU runner, interface to the alert bus, MongoDB schema) | Aryan | alerting-engineer | ongoing |
| PF-X-03 | Clone `camera_software` locally: via https "not found", ssh — the key is not registered (see PF-X-05) | Maksym Ch. | pipeline-architect | blocked `external` |
| PF-X-04 | **GitHub project**: https://github.com/valentyn-fedorov-deepx/prime_flight (private, `main`; this workspace + the `pf/` package; `external/` outside git; commits without Claude trailers) | Valentyn | pm-coordinator | done |
| PF-X-05 | **Restore access to the code**: `G:\deepx_gat` was deleted by the lead on purpose (14.09); `cv_common` @ ac5098d and `cv_trackers` @ b5d350c received as an archive (`G:\cv_common_cv_trackers.zip`) and extracted into `external/`; GM weights pulled via DVC (`gcloud auth login` done); `onnxruntime` CPU package that shadowed `onnxruntime-gpu` removed (CUDA/TensorRT providers active). Still missing: `camera_software` (PF-X-03), SSH key in GitLab for pulling updates of utils/* | Valentyn | pm-coordinator | done (partial: camera_software) |
