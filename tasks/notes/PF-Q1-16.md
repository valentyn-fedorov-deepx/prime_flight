# PF-Q1-16 · GM v2 in `pf/gm` (per ADR-001)

- **Owner:** Yurii + Valentyn · **Agent:** gm-tracker-engineer · **Status:** in-progress · **Quarter:** Q1
- **Deps:** PF-Q1-12 (done, review), PF-Q1-14 (done, review), PF-Q1-15 (tooling ready; baseline run blocked on access) · **Tags:** —
- **Repo / branch:** this repo, `pf/gm`, `pf/pipeline` · commits 91d6bfa, 0b32826, c4daaae, 3071e64 on `main`

## Context
ADR-001: one inference, two output layers. GM v2 = `gm_core` (pure frame → rows, numerically identical to v1) +
`gm_context` (incremental per-video decisions with `decided_at`) + sinks (v2 bus through `pf.contract`/`pf.receiver`,
v1-compat second-run file for the 27 modules). Source facts: `docs/analysis/gm_current.md`, `module_consumption.md`,
`contract_observed.md`.

## What has been done (journal)
- 2026-09-14 — `pf/gm/geometry.py`: exact ports of `correct_coords`, `get_overlay_ratio`, `count_bbox_frames`, letterbox
  arithmetic (1920×1080 → 1088×640 pad 224 / 1280×736 pad 272, ≈41 % padding); re-implementations of the cv_common helpers
  marked ASSUMPTION (bbox_area, bboxes_iou, get_relative_intersection = inter/area(second box), is_overlap, get_hw, get_center).
- 2026-09-14 — `pf/gm/onnx_detector.py`: `YoloV8Onnx`, same dtype flow as v1 (fp16 in, fp16 out, box math in fp16 then
  float32, class-agnostic torchvision NMS 0.7, NMS order), vectorized gather instead of the per-row GPU→CPU loop, timings.
- 2026-09-14 — `pf/gm/rows.py`: first-run rows (GM rows int-truncated + clamped; chock/vehicle rows raw floats with forced ids),
  `ClassMap` (str2id frozen as data).
- 2026-09-14 — `pf/gm/context.py`: `PartsLayout` (buckets + v1 `detect_main_plane_part`, incl. the "smallest bucket sets the bar"
  quirk), `MainAircraftTracker` (merge overlay > 0.7 drops the larger box; overlay > 0.5 re-association; longest track;
  height mode), `EntityVoter` (40 hits every 8th frame, sum map), `AircraftTypeVoter` (port of `engine_script.py`, > 500 votes),
  `CameraVoter` (running majority, explicit freeze), `VideoContextV2` aggregator with `decided_at`.
- 2026-09-14 — `pf/gm/compat_writer.py`: second-run rows exactly as `main.py:767-871` incl. the stale-`conf` quirk of
  obstacle rows and the class-2 row with `int(mode_plane_height)` last; ndjson line = `json.dumps` default (verified against
  a production line byte-for-byte).
- 2026-09-14 — `pf/pipeline/gm_stream.py`: `GmStream` — rows → context → contract frames through `Session` (gaps filled),
  events with frame ids, `write_v1_compat()`, `report()`; `scripts/gm_v2_run.py` CLI (OpenCV decoder, timings, `--compare`).
- 2026-09-14 — tests: 71 green (`pytest`, < 1 s, no torch/onnx needed).

- 2026-09-14 (afternoon) — first real-weight run. Discovery: the production files come from GM commit **a0157a4**
  (`deployment_v_py3_10`), not master; the decisive difference is **BGR→RGB** before the detector (master fed BGR). With
  the production preprocessing, the GM head matches production at 99.8 % paired detections / 97.1 % frames within
  ±2 px, ±0.02 conf on 2 795 frames; all three heads on 3 000 frames: 99.87 % pairs, 64 % bit-exact (fp16/cuDNN noise on a
  different GPU + ORT version). Details and tables: `docs/analysis/gm_prod_delta.md`. Ported since: `ImagePreprocessor`,
  tiny-plane filter, production aircraft-type vote (`pf/gm/context_prod.py`), `variant="prod"` default in `VideoContextV2`,
  tolerant parity metric (`pf.eval.compare_gm_ndjson_tolerant`), CLI `--compare-ignore-classes`.
- Speed: the 73 ms/frame figure first recorded here was an artefact of a provider option v1 does not set (cuDNN fallback
  mode) — withdrawn; corrected numbers in the night entry below and in `docs/analysis/gm_speed.md`.

- 2026-09-14 (evening) — full-video run + replay: all rows of the regenerated second-run file match production at
  99.90 % paired detections (class-2 main aircraft 99.97 %, obstacles 99.9 %, side obstacles 99.7 %), 56 % bit-exact;
  (the 79 ms/frame figure of this run is withdrawn, see `gm_speed.md` §1). Tables: `docs/analysis/gm_prod_delta.md`; raw report
  `docs/analysis/parity/gm_v2_replayDjwtQRdZyt0sSk.mp4.json`. Production main-aircraft tracking ported on a vendored
  norfair 0.2.0 core (`pf/gm/plane_tracker.py`), tracker contract in `pf/tracker/contract.py` (0 violations on 12 968
  production records).

- 2026-09-14 (night) — **L2 gate first result**: `scripts/run_module.py` (port of the stand runner, `--no-video` meta worker)
  ran the production `beltloader-chocks` module unchanged on (a) the production inferences and (b) the v2-GM second-run
  file + production tracker file of `DjwtQRdZyt0sSk`: **Pass / Pass**, identical smart timeline `[9888–22775]`, the report
  text differs by one second (00:32:13 → 00:32:14; a class-3 box at the frame edge near conf 0.35 flips one frame).
  Raw: `out/l2/*.json`.
- 2026-09-14 (night) — **speed session** (`docs/analysis/gm_speed.md`): found and fixed a wrong ORT option in the port
  (`cudnn_conv_algo_search=DEFAULT` → cuDNN fallback mode, 2.6× slower than v1's defaults); corrected baseline for the three
  heads 22.4 ms/frame; shared preprocessing + parallel heads 16.8 ms with **byte-identical rows** (600/600 per head);
  TensorRT fp16 engines 10.6 ms sequential / 8.0 ms parallel with tolerant parity (pairs within ±2 px / ±0.02: 99.0–99.2 %,
  conf Δ ≤ 0.028) — versioned change, needs the L2 gate before adoption. `pf/gm/heads.py` (`MultiHeadRunner`),
  `scripts/gm_bench.py`, `scripts/gm_ep_probe.py`; the detector now raises if TensorRT silently falls back to CUDA.
- 2026-09-14 (night) — tracker baseline profiling started (PF-Q1-17 input): weights pulled for both tracker pins (SAM +
  DeepSORT + YOLO-seg), Windows shims for `cupy`/`cuCIM` (`scripts/win_shims`, torch port of `denoise_tv_chambolle`
  bit-identical to scikit-image), `scripts/tracker_v1_profile.py`; first fact: the master pin calls the full-frame
  `estimate_sigma` on every frame with tracked objects — 161 ms per call on the CPU.

## How it was verified
- Unit tests on synthetic rows for every ported rule; letterbox numbers match `gm_current.md` §2/§7; ndjson format matches
  a real ATL-C5 line. Real weights: L1 parity vs production on the full video (`gm_prod_delta.md`), L2 gate on
  beltloader-chocks (Pass/Pass), speed configurations with exact/tolerant parity (`gm_speed.md`).
- Real-data smoke test of the parity tools (self-compare, video DjwtQRdZyt0sSk): tracker ndjson 597 MB / 37 530 frames /
  73 954 objects / 30 identities streamed in 12.7 s; GM ndjson 723 542 detections in 2.5 s; parity 1.0, no mismatches.

## Blockers (external) — resolved 14.09 12:40
- GM weights pulled via DVC (8 files); `cv_common` @ ac5098d received (str2id: 32 classes; conf 0.35 / chock 0.1 / vehicle 0.4;
  Norfair airplane: distance_threshold 0.8, initialization_delay 8, hit_inertia_max 40 — norfair 0.3.1 API); the `cv_common` helper
  assumptions in `pf/gm/geometry.py` replaced by exact ports (IoU +1 convention, int centres, relative intersection / (area+1)).
- Environment: `onnxruntime` (CPU) shadowed `onnxruntime-gpu` — removed; CUDA provider active. Real weights load on CUDA:
  GM head `[1, 29, 24276]` (25 classes), chocks/vehicle single-class heads; ~13 ms/frame GM on the RTX 5070 Ti (20-run average).

## Next
1. ~~L1 on one video~~ done. ~~cv_common helpers, Norfair adapter~~ done (norfair 0.2.0 vendored).
2. `Detectors(parallel=True)` as the default in `GmStream`; full-turnaround re-run with decode and the fixed options.
3. TensorRT adoption gate: full-video tolerant L1 + L2 (`run_module.py`) on the 7 ATL-C5 videos; engine cache policy.
4. Vehicle head re-export check (2.1× the GM head at the same size); batched postprocess.
5. L1/L2 on the other 6 ATL-C5 videos (sequential GPU runs); ADRs: camera-type freeze rule; placeholder-frame policy; `str2id` artifact.
6. Tracker v2 (PF-Q1-17) from the profiling baseline, then GM v2 + Tracker v2 in `GmStream` for the chunk-wise dataflow (PF-Q1-04).

## Decision needed
- Where v1-compat files are published (same bucket path `<video>.mp4/general_model<commit>.ndjson`?) so the tracker job and the
  modules can consume them unchanged during the transition.
- TensorRT: engines are hardware-specific → build-and-cache at deploy; which GPU runs the RT branch (T4 vs newer) decides the
  cameras-per-GPU figure and must be fixed before PF-Q2-02 sizing.
