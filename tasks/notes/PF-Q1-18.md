# PF-Q1-18 — Monthly test set on GM v2 + Tracker v2

**Owner:** Valentyn (+Yurii) · **Agents:** gm-tracker-engineer, qa-parity · **Status:** in progress (started 2026-09-14)

## Goal

Run the monthly CI test set (report of CI job 16263924101: 69 events, 90 videos, 31 tasks) through all modules on the
GM v2 + Tracker v2 inferences. The verdicts must stay the same as production while GM + Tracker cost less.

## Method

- **Reference 1 — the report's "New output"** (the CI). It cannot be reproduced exactly offline: the CI image
  (`ci-cv-module-image`, `ci.py`) is not accessible, modules are built with `cv_common_branh: master`, and the camera type
  comes from the database (`videos_by_module[...]['cameraType']` in each module's `run_module.py`).
- **Reference 2 — the control**: the same module code, flags and machine on the production inferences (GM 8576299 +
  tracker bd43c3c, the latest files in `gs://cv-modules-topics`). The paired comparison v2 vs control isolates the effect of
  GM v2 + Tracker v2 from everything else that differs from the CI.
- Per video, `scripts/testset/orchestrate.py`: fetch → GM v2 (`--variant entity_clip --parallel-heads`, tolerant L1 vs the
  production GM file) → Tracker v2 (`--exact-fast --seed 0`, L1 vs the production tracker file) → every module on the
  production inferences (`out/testset/modules/ctl`) and on the v2 inferences (`out/testset/modules/v2`).
- Task verdicts per event as the report builds them (two-camera merge Fail > Pass > Not observed; the stop-brake +
  hand-signals task derived): `scripts/testset/compare.py --runs-root .../v2 --baseline-root .../ctl`.
- Flags: camera from the report routing (`scripts/testset/plan.py`), aircraft type per event where the report implies it;
  both labels get the same flags.

## Environment deviations (identical for control and v2)

| module(s) | deviation | why the paired result is not affected |
|---|---|---|
| lead-marshaller, pushback-does-not-start, wing-walkers | `--numpy1-scalars` | NumPy 2 rejects `float()` of one-element arrays; the shim restores exactly the NumPy 1.x conversion |
| modules whose cv_common pin is not in the local archive | cv_common ac5098d2 (master) substituted, recorded in `cv_common/SUBSTITUTED_PIN.txt` | the CI builds modules with cv_common master as well |
| pre-departure walk-around | norfair 0.3.1 overlay `out/envs/norfair031` | the version pinned in the module requirements |
| post-arrival and pre-departure walk-arounds | `--drop-state-keys` for the six `transport.Airplane` stage keys and `_bl_type_bbox` / `_bl_type_frames` (full scan of a production tracker file) | the modules' own `Airplane` / `Beltloader(TrackedObject)` never read them; that cv_common copy already drops `arrival_frame` the same way |
| safety-zone | `--numpy1-scalars` ("only 0-dimensional arrays can be converted to Python scalars") | same NumPy 1.x conversion as above |
| hand-signals, steering, pin-verification | mmpose 1.x overlay `out/envs/mmpose1` (mmpose 1.3.1, mmcv-lite 2.1.0, mmengine 0.10.4 with the upstream Adafactor-registry fix, mmdet 3.2.0; two import shims documented in its README); the runner restores `pathlib.WindowsPath`, which steering aliases to `PosixPath` for the Linux image | top-down HRNet / SimCC inference uses no compiled mmcv op; on event 1 all three verdicts equal New output |
| hair-policy | WSL Ubuntu-24.04 venv `~/pf_envs/hair-policy-py38` (CPython 3.8.20, torch 2.4.1+cpu, NumPy 1.24.4, mediapipe 0.10.5) via `scripts/testset/wsl_run_module.sh`: the Pyarmor build (bytecode magic 3413) loads only on 3.8 — on master and on the `python_3_10` / `python3_10` branches alike (byte-identical `main.py` and runtime) — and no torch for 3.8 supports sm_120, so it runs on CPU | CPU instead of GPU may flip borderline classifications — identical for control and v2 |

## Findings so far

- **Control reproduces the CI**: on event 641a25728471f56e528714e9 (zHxIAF2vUGxJ) the modules on GM 8576299 + tracker
  bd43c3c match the report's New output on 11 of 12 pixel-free tasks (exception: cones_in_position fail vs Not observed,
  validated Not observed). Rejected on this event: a wing camera flag (7 of 11) and the pre-August files GM d5ef0b0 +
  tracker b5d350c (9 of 12). An earlier 9-of-12 figure for the production files came from a NumPy shim that broke
  `np.dtype(float)`; fixed before these runs.
- **GM v2 entity_clip** on zHxIAF2vUGxJ vs the production GM file: pair recall 99.93 %, frames within tolerance 92.1 %
  (the ATL-C5 runs of the prod variant, without the heavy-frame stabilizer: 55–88 %).
- **Tracker v2** on the production GM rows vs the production tracker file: 48 999 / 48 999 objects matched, 26 / 26
  identities; events equal except BL_AT_DOOR +4, BL_LEAVE +24, T_ARR +1 and T_DEP +52 frames — the tracker's own
  run-to-run randomness (production runs are not seeded; Tracker v2 is byte-identical to the pin under a shared seed).
- **First paired result** (event 641a25728471f56e528714e9, default-branch modules): on the 9 tasks with both runs the
  verdicts on GM v2 + Tracker v2 equal the control 9 / 9, and equal New output on all 12 comparable tasks.

## Which versions produced the report

- **Module code.** 18 of the 27 module repos have feature branches newer than their default branch, several with
  "Fix prod ..." commits (`out/testset/module_versions.json`). On event 641a25728471f56e528714e9 the default branches
  differ from New output on cones_in_position, pushback and handrails; `cones-placed...@not_observed_logic` (Not observed)
  and `pushback-does-not-start...@obstruction_plane_track` (Fail) reproduce New output exactly; the handrails default
  result equals the report's *Previous* column and `handrails-on-gse-being-used@per_component_improvement` gives Fail
  = New output (3 of 3 tested branches). The CI ran newer branches. `scripts/testset/calibrate.py` measures each
  candidate branch against New output on the first 5-8 events with the production inferences; the chosen checkout per
  module goes into `scripts/testset/profiles.py` (`MODULE_DIR`) and the orchestrator redoes results from another checkout.
  Module steps are paused until the choice is made; GM v2 and Tracker v2 keep running.
- **Inference files.** The safety-vests repro pack of the August runs on the same 69 events
  (`safety-vests-secured-to-body@tdv_cone:repro_2026-08/gm_inputs_manifest.json`: md5 of the 226 consumed files, bucket
  state 2026-08-01..03) names GM 087dfa9 (79 videos) + 342fd00 (11) and trackers bd43c3c (89) + b5d350c (1). On
  zHxIAF2vUGxJ the GM 087dfa9 file in the bucket still matches its md5; the bd43c3c tracker file does not: it was
  re-uploaded on 2026-08-28 (bucket versioning is off, the August file is gone), and GM 8576299 was added on 2026-09-03.
  The production tracker is not seeded, so a re-run changes private fields and event frames. The attached report does not
  record its bucket state; the current state (GM 8576299 + tracker bd43c3c of 2026-08-28) is the only complete production
  reference and stays the control.

## Cost

Timings logged during the run are under contention (8 CPU threads; one GPU shared by GM, two trackers and modules) and are
not cost figures. Clean references so far: GM heads v1 op sequence 22.4 → parallel 16.75 ms/frame, byte-identical
(TensorRT 8.0, tolerant only, ADR-003 proposed); GM v2 full turnaround 28.3 ms/frame; Tracker v1 50.6 → v2 exact-fast
39.2 ms/frame, byte-identical under a shared seed. A clean paired run on test-set videos closes this section.

## Open

- Module versions: calibration running (`out/testset/calibration*.json`), then `MODULE_DIR` and resume the module steps.
- Capacity: 8 CPU threads, 27 GB RAM, 16 GB VRAM. On 2026-09-14 19:50 RAM ran out (commit 80 of 93 GB, the WSL VM
  alone 22 GB) with GM, trackers, calibration, module runs and two environment builds in parallel; module steps were
  paused. hair-policy's Pyarmor build loads only on CPython 3.8, where no torch supports this GPU: CPU at about
  0.87 CPU-seconds per frame (≈ 4 CPU-hours per video, the `python_3_10` branch carries the same 3.8 build), so its
  180 runs (≈ 750 CPU-hours) do not fit this machine: a subset of events once GM / Tracker v2 are done, or a CPU server.
- Environments: mmpose modules (hand-signals, steering, pin-verification), hair-policy. safety-vests, safety-zone and
  safety-handrails run (safety-vests ≈ 24 min per video under contention — the module stage needs its own scheduling).
- Clean cost run (orchestrator stopped): GM v1 (production `main.py`) vs GM v2, Tracker v1 vs v2 on 2–3 test-set videos.
- Final report: per task agreement v2 vs control vs New output vs truth, analysis of every v2-vs-control mismatch, cost.
